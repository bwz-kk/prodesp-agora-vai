# Banco de Dados (PostgreSQL local via Docker — compatible com o Prisma 7)
DATABASE_URL="postgresql://postgres:prodesp123@localhost:5432/prodesp_editais"

# Credenciais do container PostgreSQL (docker-compose.yml)
DB_PASSWORD=prodesp123
DB_NAME=prodesp_editais

# Aplicação
PORT=3000
JWT_SECRET=troque-esta-chave-em-producao
NODE_ENV=development