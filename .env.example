# Banco de Dados (usado pelo Prisma e pelo docker-compose)
DATABASE_URL="mysql://root:prodesp123@localhost:3306/prodesp_editais"

# Credenciais do container MySQL (docker-compose.yml)
DB_ROOT_PASSWORD=prodesp123
DB_NAME=prodesp_editais

# Aplicação
PORT=3000
JWT_SECRET=troque-esta-chave-em-producao
NODE_ENV=development